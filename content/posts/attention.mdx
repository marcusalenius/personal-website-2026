---
title: "Attention From Scratch: Developing Intuition Behind the Core Idea in Modern AI"
lede: "How do large language models work? Together, we will develop intuition behind the core idea that makes them possible: attention."
date: 2025-08-21
maxTocLevel: 2
slug: attention
---

## The problem

Language models predict the next token from a sequence of previous tokens. Until 2017, this was usually done with recurrent networks[^rnn], which struggled with long-range dependencies — the further apart two tokens were, the harder it was to relate them.

[^rnn]: See Mikolov et al. (2010) for an early treatment of RNN-based language models.

The attention mechanism solves this elegantly: every token can look at every other token in one step.

## Scaled dot-product attention

Given queries $Q$, keys $K$, and values $V$, the attention output is:

$$
\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{Q K^\top}{\sqrt{d_k}}\right) V
$$

where $d_k$ is the dimensionality of the keys.

### Why scale by $\sqrt{d_k}$?

Without the scaling factor, large dot products push the softmax into regions with vanishingly small gradients — training stalls.

> Attention is not just an architectural detail — it's the central inductive bias of the transformer.

### Implementation sketch

A few lines of PyTorch capture the whole idea:

```python
import torch.nn.functional as F

def attention(q, k, v):
    d_k = q.size(-1)
    scores = q @ k.transpose(-2, -1) / d_k**0.5
    return F.softmax(scores, dim=-1) @ v
```

<Figure src="/images/attention-heatmap.png" alt="Heatmap of attention weights between tokens in a short sentence.">
  <Caption>Attention weights for the sentence "the cat sat on the mat".</Caption>
</Figure>

## Further reading

For background reading on the surrounding lorem, see [[lorem-ipsum]].
